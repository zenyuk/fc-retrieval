import React from "react";

import { Layout } from "antd";
const { Header } = Layout;

const TopHeader: React.FC = () => {
  return <Header className="header-color"></Header>;
};

export default TopHeader;
